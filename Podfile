# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VExtension
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'

end

target 'VideoMarks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VideoMarks
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'ReachabilitySwift'

  target 'VideoMarksTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoMarksUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'VTodayExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VTodayExtension

end
